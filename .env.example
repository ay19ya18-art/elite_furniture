# Public API base (Railway) — no trailing slash
VITE_API_URL=https://your-api.railway.app

# Admin gate (first version). Change in production.
VITE_ADMIN_EMAIL=admin@elitefurniture.com
VITE_ADMIN_PASSWORD=change-me-now

# Google Sign-In for customers (OAuth 2.0 Web client ID)
# https://console.cloud.google.com/apis/credentials
VITE_GOOGLE_CLIENT_ID=
